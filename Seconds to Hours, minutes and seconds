#include <stdio.h>

int main ()
{
    int sec,min,hr,x;
    printf("Enter the time in seconds\n");
    scanf("%d",&sec);
    hr = sec / 3600;
    x = sec % 3600;
    min = x/60;
    sec = x%60;
    printf ("%d hour %d minutes %d seconds", hr,min,sec);
    return 0;

}
